# Copy to .env and fill in. Never commit .env.
# Claude Code provides credentials itself; these are only for
# standalone scripts (e.g. the benchmark scorer) that call the API directly.

ANTHROPIC_API_KEY=sk-ant-...

# Optional: custom gateway (leave unset for Anthropic-hosted)
# ANTHROPIC_BASE_URL=
